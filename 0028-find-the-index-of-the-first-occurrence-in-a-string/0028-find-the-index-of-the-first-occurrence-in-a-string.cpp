class Solution {
public:
    int strStr(string haystack, string needle)
    {
        // METHOD - 1
        return haystack.find(needle);

        // int n = haystacks.size();
        // int m = needle.size();
        // bool startedToBeFound = false;
        // int cnt = 0; // if cnt gets m, then found, cnt inc when haystack's character match needle's in order
        // for(int i = 0; i < n; i++)
        // {
        // }
    }
};